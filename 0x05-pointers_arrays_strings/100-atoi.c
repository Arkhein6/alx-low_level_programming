#include "main.h"
#include <stdlib.h>

/**
 * _atoi - Convert a string to an integer.
 * @s: The pointer to convert
 *
 * Return: A integer
 */
int _atoi(char *s)
{
	unsigned int ni;
	n1 = atoi(s);
	return (ni);
}
